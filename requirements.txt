# tryfurqan.com runtime requirements.
#
# Furqan itself is pinned to v0.10.1, installed from the canonical
# GitHub repo. Phase 2 has zero runtime dependencies of its own; the
# pin is by tag so deploys are reproducible.
fastapi==0.118.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
furqan @ git+https://github.com/BayyinahEnterprise/furqan-programming-language.git@v0.10.1
